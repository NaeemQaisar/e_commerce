<%= form_for [@product, @comment], remote: true do |f| %>

	<div class="fields">
		<%= f.label :comment %>
		<%= f.text_field :comment %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>